apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: itemsapi-prod
type: Opaque
stringData:
  # IMPORTANT: Replace these with actual production credentials
  # Consider using AWS Secrets Manager or HashiCorp Vault for production
  DATABASE_URL: "postgresql://postgres:CHANGE_ME_PROD_PASSWORD@postgres-service:5432/itemsdb"
  POSTGRES_PASSWORD: "CHANGE_ME_PROD_PASSWORD"
